#include <stdio.h>
#include <stdlib.h>
#include <math.h>

// prototyper

void main(void)
{
}

// main menu
void printMenu() {
    // Integer value for selecting menu option
    int selection;
    int (*functionArray[])() = {f1, f2, adjust_s_services, f4};
    int menuOption[] = {1, 2, 3};
    int menuOptionCount = 3

    // Print menu options
    printf("== MENU ==\n");
    printf("1: Get a recommendation\n");
    printf("2: Adjust your streaming services\n");
    printf("3: EXIT\n");
    
    
    
    scanf("%d", &selection)
    for (int i = 0; i < menuOptionCount; i++) {
        if (selection == menuOption[0])
    }
    

    

}


// datasaet
